# Add these lines to your app/.htaccess file to set environment variables
# Generate a secure key with: openssl rand -base64 32

# Set encryption key (REQUIRED)
SetEnv HABIT_ENCRYPTION_KEY "your_generated_key_here"

# Optional: Override database credentials via environment variables
# SetEnv DB_HOST "localhost"
# SetEnv DB_NAME "habitdondb"
# SetEnv DB_USER "habitdotusr"
# SetEnv DB_PASS "your_password"
