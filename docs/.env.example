# Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit the actual .env file to version control!

# Database Configuration
DB_HOST=localhost
DB_NAME=habittracker
DB_USER=your_db_username
DB_PASS=your_db_password

# Encryption Key (REQUIRED)
# Generate using: openssl rand -base64 32
# WARNING: Never commit this key to Git! Store backup in password manager.
HABIT_ENCRYPTION_KEY=generate_your_key_here

# Usage:
# 1. Copy this file: cp .env.example .env
# 2. Generate encryption key: openssl rand -base64 32
# 3. Fill in your values in .env
# 4. For CLI scripts: source .env && php migrate_to_mysql.php
# 5. For Apache: Add SetEnv directives to .htaccess
# 6. For nginx: Add env[] directives to PHP-FPM pool config
